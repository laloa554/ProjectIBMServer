server:
  port: 8888
  
spring:
  cloud:
    config:
      enabled: true
      server:
        git: 
          uri: https://github.com/laloa554/PropertiesIBM
  profiles:
    active: development